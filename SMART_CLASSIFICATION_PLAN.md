# 作业智能分类引擎实施计划

最后更新：2026-07-30

## 1. 目标

在不破坏现有手动添加、文件扫描、归档和公示流程的前提下，为 Assignment_Dashboard 增加一套轻量、可选、可学习、便于部署的作业智能分类能力。

系统最终采用三层能力：

1. 规则算法：默认可用、纯 Python 标准库运行。
2. 本地小模型：根据用户修正样本学习个人和学校的命名习惯。
3. 云端模型：通过用户自行配置的 API Key 处理规则和本地模型无法确定的文本。

无论使用哪一层，用户手动填写的字段都拥有最高优先级，分类引擎只补全空字段或提供建议。

## 2. 当前基线

以下能力已经完成，不在后续版本中重复重构：

- 文件分类已经采用“先科目、后作业”的两层结构。
- 第一层识别 `subject_group`，第二层只在对应科目下匹配具体作业。
- 科目明确但作业不明确时进入待归档，不直接复制、归档或同步到公示目录。
- 用户可以修改科目、确认具体作业。
- 科目修正会形成轻量记忆。
- “非本作业”反馈会为对应作业记录负反馈词并影响后续算法评分。
- 两层分类回归测试已覆盖完整匹配、待归档、泛类目、多科目冲突和反馈复用。

当前分类树是界面与匹配逻辑的组织方式，底层作业记录继续保持扁平结构，避免破坏现有 API、统计、搜索和导出功能。

## 3. 明确边界

### 本计划包含

- 对已经收到的提交文件和报告文件，从文件名中预分类并提供具体作业候选。
- 规则分类器与专业规则包。
- 专业包的导入、导出、校验、预览与合并。
- 供用户复制给其他 AI 的专业包生成提示词。
- 本地可训练的小型文本分类器。
- Ollama 可选增强。
- 云端模型 API Key 接入。
- 分类灵敏度、来源、置信度和判断依据。
- 人工修正、样本管理、模型重建和恢复默认。
- 安装向导中的专业包和 AI 功能说明。

### 本计划暂不包含

- 一段通知自动拆分为多项作业。
- 作业通知、老师自然语言通知和相对日期文本的分类。
- 对复杂相对日期做完整日历推理。
- 强制要求安装 Ollama、深度学习框架或大型依赖。
- 为全国所有高校和专业维护完整课程数据库。
- AI 自动覆盖用户已经填写的科目或作业名称。

## 4. 统一分类结果

所有分类层统一返回同一种结构：

```json
{
  "assignment_id": "a123",
  "subject_group": "数字电子技术",
  "subject": "数字电子技术",
  "name": "第三次实验报告",
  "experiment": "第三次实验",
  "type": "实验报告",
  "keywords": ["数电", "数字电路", "实验报告", "第三次实验"],
  "due": "",
  "confidence": 0.92,
  "source": "rules",
  "stage": "assignment_candidate",
  "evidence": ["命中课程别名：数电", "命中类型词：实验报告"]
}
```

`source` 允许的值：`rules`、`local_model`、`ollama`、`cloud`、`feedback`、`merged`。

提交文件分类的 `stage` 允许为：`subject_candidate`、`assignment_candidate`、`pending_archive`、`confirmed`。

`assignment_id` 只在已有作业候选可以稳定映射时返回；模型不确定时返回候选列表，不直接归档。

`evidence` 只保存简短、可展示的依据，不保存模型的内部推理过程。

## 5. 分类优先级与灵敏度

### 字段优先级

```text
用户手动输入
    > 已确认的个人反馈
    > 当前模式的分类结果
    > 默认值
```

专业包提供初始知识，个人修正拥有更高优先级。任何模式都不能覆盖用户已填写的 `subject` 或 `subject_group`。

### 模式

- `off`：关闭智能补全，保留现有手动流程。
- `rules`：只使用规则和反馈记忆。
- `local_model`：规则与本地小模型共同评分。
- `ollama`：规则、本地小模型和 Ollama 共同判断，失败时自动降级。
- `cloud`：仅在用户明确配置并启用后调用云端模型，失败时自动降级。

### 算法与模型优先级

- `rules_first`：规则明确命中时直接采用，模糊时再询问模型。
- `model_first`：模型先给候选，再由规则校验确定性字段和非法结果。

默认使用 `rules_first`。

### 灵敏度

- 保守：阈值建议为 `0.85`。
- 平衡：阈值建议为 `0.70`，作为默认值。
- 积极：阈值建议为 `0.55`。
- 自定义：允许用户通过滑块调整阈值。

高于阈值的结果可以补全空字段；低于阈值时只作为建议或保持为空。

## 6. 专业规则包

### 原则

- 内置少量常见专业方向作为起点，不尝试一次性覆盖全国所有专业和高校课程。
- 第一批建议提供：自动化、电子信息、计算机、数学基础、大学英语。
- 同一课程可以保存全称、简称、英文名和学校内部俗称。
- 用户可以在安装后继续添加、修改、导入和导出。
- 专业包只补充规则，不删除已有课程、作业和反馈记录。

### 文件结构

```json
{
  "schema_version": 1,
  "profile": {
    "id": "automation-basic",
    "name": "自动化专业基础包",
    "description": "适用于自动化及相关专业",
    "generated_by": "builtin",
    "semester": ""
  },
  "subjects": {
    "自动控制原理": {
      "aliases": ["自控", "控制原理"],
      "keywords": ["根轨迹", "传递函数", "Bode", "稳态误差"],
      "assignment_types": ["课后题", "实验报告", "课程设计"]
    }
  },
  "types": {
    "实验报告": ["实验", "报告", "实验报告", "lab"],
    "课后题": ["习题", "课后题", "练习"]
  }
}
```

### AI 生成提示词

系统提供一份可复制模板，用户填写专业、学校、年级、学期和本学期正式课程名称后发送给任意 AI。课程名称由用户提供，是专业包中唯一的规范课程集合。提示词必须要求 AI：

- 只输出符合 `schema_version` 的 JSON。
- 不输出解释、Markdown 代码围栏或额外文字。
- 只能围绕用户提供的课程名称生成别名、简称、核心知识词和常见作业类型。
- 将课程别名、课程关键词、作业类型严格分开。
- 模糊联想放入待确认候选，不得直接当作确认规则。
- 不生成学生信息、路径、API Key 等隐私数据。
- 不确定的课程不得虚构，可返回空数组。

用户提供的课程名称示例：

```text
专业：自动化
年级：大二
学期：2026-2027-1
课程：自动控制原理、数字电子技术、单片机原理、工程经济学、大学英语
已知简称：数电=数字电子技术，自控=自动控制原理
```

AI 生成结果导入后，`confirmed_aliases` 可以直接参与规则分类，`suggested_aliases` 必须经过用户确认后才能启用。专业包不能因为 AI 联想而自动新增用户未提供的正式课程。

### 导入流程

```text
选择 JSON 文件或粘贴内容
    -> 解析 JSON
    -> 校验 schema_version 和字段类型
    -> 规范化、去重、限制字段长度和条目数量
    -> 展示课程与关键词预览
    -> 用户选择覆盖或合并
    -> 写入规则配置并记录来源
```

导入失败只返回具体错误，不影响服务启动和现有配置。

### 新学期初始化

```text
输入本学期课程名称
    -> 复制专业包生成提示词
    -> 用户在其他 AI 中生成 JSON
    -> 导入并校验
    -> 预览每门课程的别名、关键词和作业类型
    -> 用户删除错误联想、确认可靠联想
    -> 保存为当前学期专业包
    -> 用课程规则初始化分类器
```

专业包只负责提供冷启动知识，不覆盖已有作业、历史样本和用户反馈。旧学期课程保留但可以标记为非当前学期，分类时优先使用当前学期的活跃课程。

## 7. 本地小模型

### 定位

本地模型只处理已经收到的提交文件和报告文件，主要学习用户个人的文件命名习惯。它先预测 `subject_group`，再在该科目已有作业中预测具体作业候选和 `type`。实验编号、学生姓名、确定性关键词等字段继续由规则提取或由现有树状分类器校验。

模型不负责理解老师通知，也不负责从一段通知中拆分多项作业。训练样本不是安装、添加课程或启用分类器的必填项；零样本时专业包和规则分类器照常运行，用户只在愿意时进入管理页面增强模型。

课程识别使用一个覆盖当前活跃课程的全局多分类模型，科目状态展示的是该课程在全局模型中的样本覆盖和训练状态。具体作业识别按科目分别建立子模型，只在已确定科目的作业树中选择候选。

### 渐进式学习链路

```text
零样本
    -> 专业包 + 课程名称/别名 + 规则分类
少量样本
    -> 精确记忆 + 相似文件名检索 + 规则分类
样本达到建议量
    -> 规则 + 相似度检索 + 本地统计模型融合
置信度不足或前两名过近
    -> 候选建议或待确认，不强行归类
```

一次人工纠正应立即进入反馈记忆，不需要等待重新训练。模型训练负责把已经积累的命名规律推广到未见过但相似的文件名。

### 班级上下文与文件名拆解

文件名进入规则、相似度检索或模型前，统一经过上下文感知的拆解器。拆解器读取系统已有数据：

- 复用当前配置中的 `class_name`，不另建重复班级名称字段。
- 允许补充可选的班级别名，例如“电国241”“电气国贸24-1”。
- 读取学生列表中的姓名和学号，只用于识别、清洗和展示依据。
- 读取当前学期课程名称、确认别名、课程关键词和已有作业节点。

推荐拆解顺序：

1. 规范化全角半角、大小写、分隔符和扩展名。
2. 优先识别“学号 + 姓名”组合，再识别处于开头、结尾或明确分隔符边界的姓名。
3. 识别班级名称和班级别名。
4. 清除“最终版、修改版、副本、微信文件”等无关版本标记。
5. 保留课程名称、课程别名、作业类型、实验编号和作业编号。
6. 无法可靠判断的片段保持原样，避免误删课程术语。

班级、姓名和学号只作为负向清洗上下文，不能作为课程预测的正向特征。姓名孤立出现在专业术语内部时不得直接删除，避免误伤“电力电子技术”等课程词。发送给 Ollama 或云端模型的默认内容也只能是清洗后的分类文本，不包含学生名单、姓名、学号、班级信息和本地路径。

### 特征与分类方法

首版不微调大语言模型，采用适合小样本、可由 Python 标准库实现的轻量方法：

- 中文字符 2 至 4 gram。
- 英文单词和数字序号特征。
- 课程别名、作业类型和实验编号特征。
- 字符 n-gram 相似度检索，优先复用已确认的相似文件名。
- Complement Naive Bayes 或可序列化的加权统计分类器，降低课程样本数量不均衡的影响。
- 只使用 Python 标准库训练和推理。

融合结果不能只判断最高分是否超过阈值，还需要检查第一名与第二名的分差、规则与模型是否强冲突以及目标课程是否处于当前学期。未知课程和差距过小的结果必须进入待确认。

### 数据文件

- `data/ai_examples.json`：用户确认、修正和专业包预热样本。
- `data/ai_rules.json`：当前启用的规则和专业包合并结果。
- `data/models/course_model.json.gz`：当前活跃课程的全局分类模型。
- `data/models/assignment_models/`：各科目对应的作业候选子模型。
- `data/models/model_meta.json`：模型版本、特征版本、训练时间、样本统计和验证结果。

示例样本：

```json
{
  "raw_name": "电国241_20241234_张三_数电实验三最终版.docx",
  "normalized_text": "数电 实验三",
  "removed": {
    "class_name": "电国241",
    "student_name": "张三",
    "student_id": "20241234",
    "noise": ["最终版"]
  },
  "corrected_result": {
    "subject_group": "数字电子技术",
    "type": "实验报告",
    "experiment": "第三次实验"
  },
  "created_at": "2026-07-30T12:00:00",
  "source": "manual_feedback",
  "weight": 1.2,
  "preprocess_version": 1
}
```

原始文件名必须保留。班级名称、学生列表或拆解规则变化后，系统可以从原始名称重新生成特征并重建模型，不要求用户重新投喂。

样本来源建议权重：

- 用户纠正结果：`1.2`。
- 用户主动确认：`1.0`。
- 专业包中的真实样本：`0.8`。
- AI 生成的模拟文件名：`0.3`，只负责冷启动。
- 未经确认的自动分类：不进入正式训练集。

### 学习流程

1. 用户接受或修改分类结果。
2. 系统保存原始文件名、拆解结果、最终确认字段、来源、权重和预处理版本。
3. 少量样本先通过精确记忆和相似文件名检索立即生效。
4. 达到建议样本量后，从全部有效样本重建模型，保证结果可复现。
5. 专业包生成样本与真实确认样本分组，模型验证优先使用未参与训练的真实样本。
6. 新模型先写入临时文件并验证，通过后原子替换；训练失败时继续使用上一版模型。
7. 用户可以查看、删除、重新拆解、重建模型或清空个人学习数据。

防止错误样本污染：未经用户确认的自动结果不能直接作为训练样本。

默认建议每门课程少于 5 条确认样本时显示“积累中”，达到 10 条后显示“可训练”，训练后新增 5 条有效样本时显示“待更新”。这些数字是可调整建议值，不是功能解锁条件。样本不足时继续使用规则、专业包和相似度记忆。

### 训练器与分发

训练能力作为后端模块随项目分发，不额外提供要求用户操作的独立训练软件：

- `ai_classifier.py`：统一分类入口、模型加载、推理与融合。
- `classifier_features.py`：班级上下文加载、文件名拆解和特征提取。
- `classifier_trainer.py`：训练、验证、模型导出、回滚和状态统计。

这三个模块进入项目仓库、安装向导和更新包。用户的 `data/ai_examples.json`、`data/ai_rules.json` 和 `data/models/` 属于运行时数据，安装和升级不得覆盖。训练在后台任务中执行，不能阻塞 Dashboard；训练完成后热加载新模型，无需重启。

## 8. Ollama 与云端模型

### Ollama

- 默认关闭，不作为安装依赖。
- 使用 `urllib.request` 调用 `http://localhost:11434/api/generate`。
- 可配置地址、模型和超时时间。
- 推荐轻量模型只作为界面建议，不在代码中强制下载。
- 响应必须经过 JSON 提取、字段白名单、类型校验和置信度裁剪。
- 连接失败、超时、模型不存在或响应异常时，自动回退到规则或本地模型。

### 云端模型

- 默认关闭，用户主动填写 API Key 后才允许调用。
- 设置页明确显示将发送的字段和隐私说明。
- 默认只发送待分类文本和允许的课程候选，不发送文件内容、学生名单和本地路径。
- API Key 只保存在本机配置中，日志和接口响应不得回显完整 Key。
- 云端响应同样经过严格 JSON 校验，不能直接写入作业数据。

## 9. 后端结构与接口

新增逻辑优先放入 `ai_classifier.py`，减少对 `server.py` 主流程的改动。

建议函数：

```python
classify_assignment(text, name="", subject="", experiment="", config=None)
normalize_assignment_filename(name, class_context=None)
rules_classify(text, rules=None)
similarity_classify(text, examples=None)
local_model_classify(text, model=None)
ollama_classify(text, config=None)
cloud_classify(text, config=None)
merge_result(rule_result, model_result, config=None)
save_feedback(text, corrected_result)
rebuild_local_model()
get_course_model_status()
validate_rule_pack(payload)
import_rule_pack(payload, merge=True)
```

新增接口：

- `POST /api/assignment/classify`：提交文件预分类，不保存作业或归档文件。
- `POST /api/assignment/feedback`：保存用户最终确认结果并更新学习数据。
- `GET /api/ai/settings`：读取可公开的分类设置和运行状态。
- `POST /api/ai/settings`：保存模式、优先级、灵敏度和模型配置。
- `POST /api/ai/rules/import`：校验、预览或导入专业包。
- `GET /api/ai/rules/export`：导出当前自定义专业包。
- `GET /api/ai/feedback`：查看学习记录。
- `POST /api/ai/model/rebuild`：重建本地模型。
- `GET /api/ai/model/status`：读取全局训练状态和各科目样本覆盖状态。
- `POST /api/ai/model/cancel`：取消尚未完成的后台训练。
- `POST /api/ai/filename/inspect`：预览班级、学生、学号、噪声词和有效分类文本的拆解结果。
- `POST /api/ai/model/reset`：清除个人模型数据，必须二次确认。

`/api/assignment/add` 只做兼容增强：调用分类器后合并关键词，并可补全空的 `experiment`、`type` 等字段；不得覆盖用户填写的科目。提交文件的智能分类走待归档和人工确认链路，不直接改变已有作业配置。

所有 AI 模块使用安全导入。模块加载或运行失败时，`server.py` 必须照常启动。

## 10. 前端计划

第一阶段优先在新版和旧版的待归档文件区域增加“智能识别/重新判断”入口：

1. 用户选择一个已经收到的提交文件或报告文件。
2. 点击后调用 `/api/assignment/classify`。
3. 显示建议科目、具体作业候选、置信度、来源和简短依据。
4. 只有高置信度且通过现有树状规则校验的结果才允许进入确认归档。
5. 用户确认或修正后，将最终结果保存为学习样本。

新增作业表单的自然语言“智能识别”按钮暂缓，等提交文件分类稳定后再评估是否需要。

管理或设置页面增加：

- AI 模式选择。
- 规则优先或模型优先。
- 灵敏度预设和自定义滑块。
- “班级上下文”区域：复用并允许修改当前班级名称，维护可选班级别名，显示已载入学生和学号数量。
- 文件名拆解预览：展示原始名称、识别出的班级/学生/学号、移除的噪声和最终分类文本，允许用户修正后加入记忆。
- “学习增强”入口：扫描已有作业目录、导入文件名列表、拖入代表性文件、查看样本和触发训练，全部可跳过。
- 每门课程显示课程识别状态、作业节点模型状态、人工确认样本数、生成样本数、新增待训练数量和最近训练时间。
- 状态统一为“仅规则、积累中、可训练、训练中、模型已就绪、待更新、训练失败”。
- 验证样本不足时显示“验证数据不足”，不展示误导性的准确率。
- Ollama 连接状态和模型名。
- 云端模型配置与隐私说明。
- 专业包导入、导出和 AI 提示词复制入口。
- 学习记录查看、删除、重建和重置入口。

现有“科目 -> 作业”树状展示继续使用 `subject_group` 动态分组，不将作业配置改造成嵌套存储。

分类大脑顶部的模型层可以汇总“已就绪课程数、正在积累课程数、已确认样本数和待训练样本数”，课程详情页再展示具体训练信息，避免主界面过载。

## 11. 安装向导计划

安装向导修改放在模型功能版本中统一完成。建议流程：

1. 基础安装与目录配置。
2. 可选的专业方向选择，可直接跳过。
3. 选择内置专业包、导入专业包或稍后配置。
4. 展示“让其他 AI 生成专业包”的说明和提示词入口。
5. AI 模式默认选择规则模式。
6. 检测 Ollama，但不自动安装、不阻塞安装。
7. 云端 API Key 保持为空并默认关闭。
8. 安装完成页说明专业包和 AI 设置均可在管理页面修改。

安装向导不得把 AI、Ollama 或云端 Key 设为完成安装的必要条件。

## 12. 分阶段实施

### 阶段 A：当前版本收尾

状态：已完成基线，后续只做回归修复。

- 保持两层算法分类稳定。
- 保持待归档文件在人工确认前不写入归档目录。
- 维护现有 7 项回归测试。
- 不在当前版本接入模型或修改安装向导。

验收：现有分类测试持续通过，手动添加和原有扫描流程无回归。

### 阶段 B：规则引擎与专业包

状态：已完成。

- 新建 `ai_classifier.py`。
- 建立统一分类结果格式。
- 实现规则分类和反馈优先匹配。
- 实现专业包校验、导入、导出和 AI 生成提示词。
- 新增 `/api/assignment/classify`。
- 新增待归档文件的智能重新判断入口。
- 分类结果必须经过现有“科目 -> 作业”树状规则校验。
- `/api/assignment/add` 只补全关键词和其他空字段。

验收：完全不安装模型时，可以根据专业包识别常见提交文件的科目和作业类型；异常专业包不会破坏配置，低置信度结果不会直接归档。

### 阶段 C：本地小模型与学习管理

状态：已完成，并已扩展批量投喂与完整样本库。

- 实现班级上下文加载、学生名单匹配和可重放的文件名拆解。
- 实现训练样本保存、精确记忆和字符 n-gram 相似度检索。
- 实现全局课程模型、各科目作业子模型和本地模型重建。
- 增加随仓库、安装器和更新包分发的标准库训练器。
- 实现规则优先、模型优先和融合评分。
- 增加灵敏度设置。
- 增加学习增强入口、按科目训练状态、后台训练进度和失败回退。
- 增加学习记录查看、删除、重新拆解、重建和重置。
- 将现有科目修正和负反馈迁移或映射为可利用样本。

验收：不提供训练样本时规则分类照常运行；一次修正后相似文件立即受益；班级名、学生姓名和学号不会成为课程正向特征；删除样本并重建后结果可复现；训练失败不会破坏上一版模型。

### 阶段 D：Ollama 与云端模型

- 增加 Ollama 检测和调用。
- 增加云端模型适配层与 API Key 设置。
- 完成超时、异常 JSON、字段污染和降级测试。
- 展示分类来源和隐私提示。

验收：没有 Ollama、断网、Key 错误和模型异常时，项目仍可正常启动并回退到本地分类。

### 阶段 E：安装向导与发布

- 根据届时的安装向导代码接入专业包选择和 AI 功能说明。
- 打包内置专业包、提示词模板和必要数据目录。
- 检查升级安装不会覆盖用户规则、学习样本和 API Key 配置。
- 完成新安装、旧版本升级和无模型环境测试。

验收：用户可以跳过全部 AI 配置完成安装，也可以在安装期间选择专业包，并在安装后继续修改。

## 13. 测试清单

- 规则分类：课程别名、英文词、实验序号、作业类型和关键词去重。
- 优先级：用户字段不会被任何分类来源覆盖。
- 灵敏度：三个预设与自定义阈值行为正确。
- 专业包：合法导入、损坏 JSON、旧 schema、重复课程、超长字段和覆盖/合并。
- 反馈：保存、删除、重建、重置和相似文本复用。
- 文件名拆解：班级名、班级别名、姓名、学号、姓名与专业术语冲突、版本词、无分隔符文件名和名单更新后重新拆解。
- 本地模型：空样本、单类样本、小样本、课程样本不均衡、未知课程、前两名分差过小、训练取消、验证失败和上一版模型回退。
- 样本来源：人工纠正、人工确认、专业包真实样本、AI 模拟样本的权重与隔离验证。
- 模型状态：仅规则、积累中、可训练、训练中、已就绪、待更新和训练失败。
- 新科目冷启动：只有一两个文件时的课程联想、用户确认、候选升级和未知科目保护。
- 新学期初始化：课程名称约束、AI 生成专业包、别名冲突、关键词误分类和当前学期优先级。
- Ollama：服务不存在、超时、模型不存在、非 JSON 响应和字段缺失。
- 云端：无 Key、错误 Key、超时、限流和异常响应。
- 降级：任意 AI 失败都不影响服务启动、手动新增和规则分类。
- 隐私：日志不包含完整 API Key、学生名单、本地路径或未经允许的文件内容。
- 兼容：旧版和新版前端、原有 `/api/assignment/add`、现有配置文件与更新流程。

## 14. 开发约定

- 规则分类和本地小模型首版只使用 Python 标准库。
- Ollama 调用使用 `urllib.request`，不新增 `requests` 等依赖。
- 每次代码修改都同步记录到 `CONVERSATION_LOG.md`。
- 每个阶段独立提交和验收，避免规则、模型、云端与安装向导一次性混改。
- 运行时数据写入 `data/`，仓库内只提交默认模板或生成脚本，不提交用户真实样本和 Key。
- 所有新配置都有默认值，并支持旧配置自动补全。

## 15. 完整分类链路

### 15.1 新学期专业包准备

用户先输入本学期正式课程名称，可选填写专业、年级、学校和已知简称。系统生成一份可复制的 AI 提示词，用户可以发送给任意其他 AI，让其补充课程别名、关键词和常见作业类型。

```text
本学期课程名称
    -> AI 联想别名和课程关键词
    -> JSON 导回系统
    -> schema 和字段校验
    -> 用户预览、删除、修改、确认
    -> 生成当前学期专业包
```

这里的正式课程名称由用户决定，AI 只能补充信息，不能擅自新增正式课程。可靠别名进入 `confirmed_aliases`，不确定联想进入 `suggested_aliases`，确认前不参与自动分类。

### 15.2 提交文件进入系统

当前系统收到的对象只有提交文件和报告文件，不处理作业通知。第一版默认只使用文件名，不读取文档正文。

```text
收到文件
    -> 提取文件名主体
    -> 读取当前班级名称/别名和学生列表
    -> 优先识别学号+姓名、班级标记和明确边界中的姓名
    -> 去除扩展名、已确认学生信息、日期、版本号等噪声
    -> 保留课程别名、课程关键词、作业类型和实验序号候选
    -> 保存原始名称、清洗结果和预处理版本
```

学生姓名、学号和班级名称只用于清洗，不能进入课程模型的正向特征。无法可靠确认的片段不删除；名单或拆解规则变化后，使用保存的原始名称重新拆解。云端增强默认只接收清洗后的分类文本。

### 15.3 第一层：科目识别

规则分类器先检查当前活跃科目的规范名称、确认别名、专业包关键词和用户反馈。

```text
文件名
    -> 直接命中规范课程名或确认别名：高置信度科目
    -> 命中专业包关键词：候选科目
    -> 命中用户历史样本：反馈优先
    -> 多个科目同时命中：科目冲突
    -> 没有可靠命中：未知科目
```

未知科目不能强行归入“其他”后自动归档，而应进入待分类区域。

### 15.4 第二层：具体作业候选

科目确定后，只在该科目的现有作业树中继续判断，避免跨科目误匹配。

```text
已确认科目
    -> 匹配作业名称、实验名称、关键词和序号
    -> 规则评分
    -> 本地模型评分
    -> 合并候选
```

模型可以返回多个候选，但只有具体作业唯一、置信度达到阈值并通过现有树状规则校验时，才允许进入确认归档。

### 15.5 第三层：本地小模型

本地模型读取文件名特征、专业包联想和用户确认样本，分别预测科目与具体作业候选。它不直接拥有归档权限，只提供候选、置信度和判断依据。

```text
精确反馈记忆
 历史相似样本检索
 规则结果
 字符 n-gram / Complement Naive Bayes 分类结果
    -> 融合评分
    -> 检查最高分、前两名分差、规则冲突和当前学期
    -> 根据灵敏度决定自动建议或人工确认
```

新科目只有一两个样本时，优先使用确认别名和相似样本记忆；样本逐步积累后才提高统计模型权重。训练样本始终可选，零样本课程保持“仅规则”状态，不影响其他课程和项目启动。

### 15.6 可选增强层

只有规则和本地模型无法稳定判断时，才根据配置选择 Ollama 或云端模型。它们只返回候选，不绕过现有规则和用户确认。

```text
规则明确
    -> 直接使用
规则模糊
    -> 本地小模型
本地仍然模糊
    -> Ollama 或云端模型（用户主动开启时）
任何增强层失败
    -> 回退到本地模型和规则
```

### 15.7 用户确认与归档

```text
候选唯一且高置信度
    -> 用户确认
    -> 归档文件
    -> 同步公示目录（若功能启用）

候选不唯一、科目未知或置信度不足
    -> 进入待归档
    -> 用户修改科目或选择具体作业
    -> 用户确认后再归档
```

现有“先科目、后作业”的树状算法是最终安全门槛，AI 结果不能直接绕过它。

### 15.8 反馈与持续学习

用户确认或修改后的结果才可以成为正式训练样本：

```text
用户确认/修改
    -> 保存原始文件名、拆解结果、最终科目、具体作业、来源和权重
    -> 立即更新精确记忆和相似样本索引
    -> 达到条件后在后台重建轻量模型
    -> 验证通过后原子切换，失败则保留上一版
    -> 后续相似文件优先参考新样本
```

系统需要允许用户查看、删除、重新拆解、重建和重置学习数据。自动猜测但未被用户确认的结果不能直接进入训练集。AI 生成样本只作为低权重冷启动数据，不能替代真实确认样本完成模型验证。

### 15.9 统一状态

每个提交文件在链路中保持清晰状态：

```text
new
  -> subject_candidate
  -> assignment_candidate
  -> pending_archive
  -> confirmed
  -> archived
```

科目冲突和未知科目属于待人工处理状态，不作为失败启动或异常退出。

## 16. 后续版本开始位置

阶段 B 的规则引擎与专业包、阶段 C 的本地小模型与学习管理均已完成。下一代码阶段从阶段 D 开始：

1. 建立统一的可选增强适配层，不让 Ollama 或云端逻辑进入现有规则与本地模型核心。
2. 先实现 Ollama 可用性检测、超时、JSON 校验和自动降级。
3. 再实现云端 API Key 的本地保存、模型适配、限流和错误降级。
4. 两类增强结果继续经过科目树、作业树、灵敏度和分差校验，只能提供候选。
5. 阶段 D 验收后再进入阶段 E，修改安装向导并生成正式更新包。

当前代码进入正式发布前，还需要在确定版本号后重建安装器和更新包，并执行新安装、旧版本升级及用户数据不覆盖验收。

## 17. 样本管理与批量投喂工作台

### 17.1 已落实范围

- 现代版“学习增强”拆分为“快速预热、批量投喂、样本库”三个子页，经典版继续跳转现代分类大脑。
- 快速预热支持扫描能映射到当前有效作业的历史归档记录；历史记录只生成候选，确认后才进入训练集。
- 批量投喂统一接收文件、文件夹、拖放和粘贴文件名列表，最多 2,000 条；只发送文件名和相对目录，不读取正文、不保存完整路径。
- 候选预览按 50 条分页，显示原始名称、移除项、规范化文本、课程、具体作业和状态；无冲突项默认选中，冲突项默认跳过并保留。
- 支持整批应用默认课程和作业、逐行修正规范化文本与标签、只看冲突、选择可加入项及导入结果统计。
- 单文件拆解器允许编辑规范化文本、课程和作业，并可直接“确认并加入样本”。
- 样本库使用服务端分页，支持文件名搜索、课程、作业、来源和预处理版本筛选；支持详情查看、人工编辑、批量删除及选择/全部重新拆解。
- 人工编辑样本使用 `manual_correction` 来源和 `1.2` 权重；普通确认及批量导入使用 `1.0` 权重。

### 17.2 数据一致性

- `ai_examples.json` schema 升级为 v2，并增加单调递增的 `data_version`。
- 批量导入通过一次内存合并、一次原子写入和一次自动训练调度完成。
- 相同文件和标签合并计数；不同标签进入冲突状态，人工编辑时以最近一次确认结果替换旧标签。
- 重新拆解默认保留人工规范化文本，用户明确关闭“保留人工规范文本”后才允许覆盖。
- 样本新增、编辑、删除或重新拆解后，已训练模型与样本数据版本不一致时标记为“待更新”，旧模型立即退出融合。
- 过期模型文件仍保留用于文件损坏层面的备份，但训练失败不会恢复使用与当前数据版本不一致的预测结果。
- 精确记忆、Dice 相似度和规则分类不依赖统计模型版本，样本确认后立即生效。

### 17.3 接口

- `POST /api/ai/examples/preview`：按 `history` 或 `filenames` 生成批量候选。
- `POST /api/ai/examples/import`：兼容旧参数，并支持逐行确认的 `items` 原子导入。
- `GET /api/ai/examples`：增加作业、搜索和预处理版本筛选，并返回来源、课程和版本汇总。
- `POST /api/ai/examples/update`：人工更新课程、作业和规范化文本。
- `POST /api/ai/examples/reprocess`：支持指定样本 ID及保留/覆盖人工规范化文本。
- 所有批量接口限制数量和字段长度，错误明细最多返回 50 条，响应中不包含完整本地路径。

### 17.4 验收状态

- Python 与现代版内联 JavaScript 静态检查通过。
- 全量 56 项单元测试通过，覆盖批量版本写入、筛选分页、一次训练调度、冲突处理、人工修正、重新拆解、历史有效作业过滤和过期模型停用。
- Playwright 已检查现代版桌面和 390px 移动端的三个子页、粘贴预览、样本筛选和编辑弹窗；页面无横向溢出，控制台无错误或警告。
- 2,001 条前端输入验收通过：只发送和展示前 2,000 条，保留真实总数并明确提示分批处理。
- 分发回归测试继续确认安装器、PyInstaller 和更新包包含训练模块，但不携带运行时样本和模型。
